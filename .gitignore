.DS_Store
__pycache__/
*.pyc

# generated PIV material: never commit. secrets.h holds real private keys in
# plaintext; only the .example.h templates are tracked.
*.pem
keys/
*/main/secrets.h
*/secrets.h

# build output
*/build/
*/managed_components/
*/sdkconfig
*/sdkconfig.old
.cache/

# bench build tree (BUTTON_AUTHENTICATES=ON) — never a shipping image
build-bench/

# Vendor documentation: kept locally, never redistributed.
*.pdf
